<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{{ title or site.name }} | {{ site.name }}</title>
    <meta name="description" content="{{ pkg.description }}" />

    <link rel="canonical" href="{{ page.url | htmlBaseUrl(pkg.author.url) }}" />

    <link
      rel="stylesheet"
      href="https://stevecherry.net/normcore/css/normcore.css"
    />
    <link rel="stylesheet" href="/public/style.css" />

    <script
      src="https://stevecherry.net/normcore/js/normcore.js"
      defer
    ></script>
    <script src="/public/script.js" defer></script>
  </head>
  <body>
    <div class="container cover stack">
      <header role="banner">
        <a href="/">{{ site.name }}</a>
      </header>

      <main class="[ stack ] [ flex-auto ]" role="main">
        <h1>{{ title or site.name }}</h1>

        {{ content | safe }}
      </main>

      <footer class="stack" role="contentinfo">
        <p>{{ pkg.description }}</p>
        <p>Yet <a href="{{ pkg.homepage }}">another project</a> by <a href="{{ pkg.author.url }}">{{ pkg.author.name }}</a>.</p>
      </footer>
    </div>
  </body>
</html>
